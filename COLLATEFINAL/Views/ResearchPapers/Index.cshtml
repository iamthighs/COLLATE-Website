@model ResearchPaginatedResult<ResearchPapersModel>
@inject SignInManager<AppIdentityUser> SignInManager
@inject UserManager<AppIdentityUser> UserManager

@{
    ViewData["Title"] = "Research Papers";
    ViewData["ActivePage"] = ManageSidebar.ResearchPapers;
    
}

<partial name="_NewNavbar" />
<div id="layoutSidenav">
    <partial name="_NewSidenav" />
    <div id="layoutSidenav_content">
        <main>
            <!-- Main page content-->
            <div class="container-xl px-4 mt-4">
                <header class="card card-waves">
                    <div class="card-body px-5 pt-5 pb-0">
                        <div class="row align-items-center justify-content-between">
                            <div class="col-lg-8">
                                <h1 class="text-primary">Research Papers</h1>
                                <p class="lead mb-4">
                                    In this page, we are presenting the Research projects of Students of Computer Engineering with Natural Excellence organization that consist of their project proposal, research model and design, framework, and methodology. Our team specifically intends to provide a compilation of this engaging and innovative research projects to guide our fellow computer engineering students build their own research proposals. The whole process of research opens new doors of learning and literary growth so we are aiming to give them variety of ideas for the topics that they want to pursue. By comparing and contrasting the findings of other studies, it highlights how and in what ways their chosen research proposal differs from other research about the topic.
                                </p>
                                <div class="shadow rounded">
                                    <form asp-action="Index" method="get">
                                        <div class="input-group input-group-joined input-group-joined-xl border-0">
                                            <input class="form-control me-0" type="text" name="s" value="@Model.SearchKeyword" placeholder="Search..." aria-label="Search" autofocus />
                                            <span class="input-group-text"><i data-feather="search"></i></span>
                                        </div>
                                    </form>
                                </div>
                            </div>
                            <div class="col-lg-4"><img class="img-fluid" src="/new/assets/img/illustrations/problem-solving.svg" /></div>
                        </div>
                        <nav class="nav nav-borders">
                            <a class="nav-link active ms-0" href="#!">Getting Started</a>
                            <a class="nav-link" href="#!">Tutorials</a>
                            <a class="nav-link" href="#!">Licensing</a>
                            <a class="nav-link" href="#!">Terms &amp; Conditions</a>
                            <a class="nav-link" href="#!">Support</a>
                        </nav>
                    </div>
                </header>
                <h4 class="mb-0 mt-5">Thesis</h4>
                <hr class="mt-2 mb-4" />
                <div class="row">
                    @if (Model.TotalCount == 0)
                    {
                        <div class="col-4 text-center">
                        </div>
                        <div class="col-4 text-center">

                            <p>Your search - @Model.SearchKeyword - did not match any entry.</p>

                        </div>
                        <div class="col-4 text-center">
                        </div>
                    }
                    else
                    {

                        @foreach (var item in Model.Result)
                        {
                            @if(item.Header == "Thesis")
                            {
                                <div class="col-lg-4 mb-4">
                                    <!-- Knowledge base category card-->
                                    <a class="card lift shadow-none rounded-xl" href="@Url.Action("ResearchPapersDetail","Detail", new { id = item.Id, pageType = "ResearchPapers"})">
                                        <img class="card-img-top" src="@Url.Content($"~/Uploads/ResearchPapers/{item.ImageUrl}")" alt="...">
                                        <div class="card-body">
                                            <h5 class="card-title text-teal">
                                                <i class="me-2" data-feather="file-text"></i>
                                                @item.Title
                                            </h5>
                                        </div>
                                        <div class="card-footer"><div class="small text-muted">@item.PostedDate.ToString("MMM dd, yyyy")</div></div>
                                    </a>
                                </div>
                            }
                        }
                    }
                </div>

                <h4 class="mb-0 mt-5">Case Studies</h4>
                <hr class="mt-2 mb-4" />
                <div class="row">
                    @if (Model.TotalCount == 0)
                    {
                        <div class="col-4 text-center">
                        </div>
                        <div class="col-4 text-center">

                            <p>Your search - @Model.SearchKeyword - did not match any entry.</p>

                        </div>
                        <div class="col-4 text-center">
                        </div>
                    }
                    else
                    {

                        @foreach (var item in Model.Result)
                        {
                            @if (item.Header == "Case Study")
                            {
                                <div class="col-lg-4 mb-4">
                                    <!-- Knowledge base category card-->
                                    <a class="card lift shadow-none rounded-xl" href="@Url.Action("ResearchPapersDetail","Detail", new { id = item.Id, pageType = "ResearchPapers"})">
                                        <img class="card-img-top" src="@Url.Content($"~/Uploads/ResearchPapers/{item.ImageUrl}")" alt="...">
                                        <div class="card-body">
                                            <h5 class="card-title text-teal">
                                                <i class="me-2" data-feather="file-text"></i>
                                                @item.Title
                                            </h5>
                                        </div>
                                        <div class="card-footer"><div class="small text-muted">@item.PostedDate.ToString("MMM dd, yyyy")</div></div>
                                    </a>
                                </div>
                            }
                        }
                    }
                </div>

                <h4 class="mb-0 mt-5">Hardware</h4>
                <hr class="mt-2 mb-4" />
                <div class="row">
                    @if (Model.TotalCount == 0)
                    {
                        <div class="col-4 text-center">
                        </div>
                        <div class="col-4 text-center">

                            <p>Your search - @Model.SearchKeyword - did not match any entry.</p>

                        </div>
                        <div class="col-4 text-center">
                        </div>
                    }
                    else
                    {

                        @foreach (var item in Model.Result)
                        {
                            @if (item.Header == "Hardware")
                            {
                                <div class="col-lg-4 mb-4">
                                    <!-- Knowledge base category card-->
                                    <a class="card lift shadow-none rounded-xl" href="@Url.Action("ResearchPapersDetail","Detail", new { id = item.Id, pageType = "ResearchPapers"})">
                                        <img class="card-img-top" src="@Url.Content($"~/Uploads/ResearchPapers/{item.ImageUrl}")" alt="...">
                                        <div class="card-body">
                                            <h5 class="card-title text-teal">
                                                <i class="me-2" data-feather="file-text"></i>
                                                @item.Title
                                            </h5>
                                        </div>
                                        <div class="card-footer"><div class="small text-muted">@item.PostedDate.ToString("MMM dd, yyyy")</div></div>
                                    </a>
                                </div>
                            }
                        }
                    }
                </div>

                <h4 class="mb-0 mt-5">Software</h4>
                <hr class="mt-2 mb-4" />
                <div class="row">
                    @if (Model.TotalCount == 0)
                    {
                        <div class="col-4 text-center">
                        </div>
                        <div class="col-4 text-center">

                            <p>Your search - @Model.SearchKeyword - did not match any entry.</p>

                        </div>
                        <div class="col-4 text-center">
                        </div>
                    }
                    else
                    {

                        @foreach (var item in Model.Result)
                        {
                            @if (item.Header == "Software")
                            {
                                <div class="col-lg-4 mb-4">
                                    <!-- Knowledge base category card-->
                                    <a class="card lift shadow-none rounded-xl" href="@Url.Action("ResearchPapersDetail","Detail", new { id = item.Id, pageType = "ResearchPapers"})">
                                        <img class="card-img-top" src="@Url.Content($"~/Uploads/ResearchPapers/{item.ImageUrl}")" alt="...">
                                        <div class="card-body">
                                            <h5 class="card-title text-teal">
                                                <i class="me-2" data-feather="file-text"></i>
                                                @item.Title
                                            </h5>
                                        </div>
                                        <div class="card-footer"><div class="small text-muted">@item.PostedDate.ToString("MMM dd, yyyy")</div></div>
                                    </a>
                                </div>
                            }
                        }
                    }
                </div>

                <h4 class="mb-0 mt-5">Others</h4>
                <hr class="mt-2 mb-4" />
                <div class="row">
                    @if (Model.TotalCount == 0)
                    {
                        <div class="col-4 text-center">
                        </div>
                        <div class="col-4 text-center">

                            <p>Your search - @Model.SearchKeyword - did not match any entry.</p>

                        </div>
                        <div class="col-4 text-center">
                        </div>
                    }
                    else
                    {

                        @foreach (var item in Model.Result)
                        {
                            @if (item.Header == "Others")
                            {
                                <div class="col-lg-4 mb-4">
                                    <!-- Knowledge base category card-->
                                    <a class="card lift shadow-none rounded-xl" href="@Url.Action("ResearchPapersDetail","Detail", new { id = item.Id, pageType = "ResearchPapers"})">
                                        <img class="card-img-top" src="@Url.Content($"~/Uploads/ResearchPapers/{item.ImageUrl}")" alt="...">
                                        <div class="card-body">
                                            <h5 class="card-title text-teal">
                                                <i class="me-2" data-feather="file-text"></i>
                                                @item.Title
                                            </h5>
                                        </div>
                                        <div class="card-footer"><div class="small text-muted">@item.PostedDate.ToString("MMM dd, yyyy")</div></div>
                                    </a>
                                </div>
                            }
                        }
                    }
                </div>

                <!-- Content Row -->
                <div class="row">

                    <!-- Content Column -->
                    <div class="col-lg-12 mb-4">
                        <nav class="float-right">
                            <ul class="pagination jusify-content-center">
                                <li class="page-item @(Model.Page == 1 ? "disabled" : "")">
                                    <a class="page-link" asp-route-s="@Model.SearchKeyword" asp-route-p="@(Model.Page - 1)">
                                        Previous
                                    </a>
                                </li>
                                @for (var pageNumber = 1; pageNumber <= Model.TotalCount; pageNumber++)
                                {
                                    if (pageNumber == Model.Page)
                                    {
                                        <li class="page-item active">
                                            <span class="page-link">@pageNumber</span>
                                        </li>
                                    }
                                    else
                                    {
                                        <li class="page-item">
                                            <a class="page-link" asp-route-s="@Model.SearchKeyword" asp-route-p="@pageNumber">@pageNumber</a>
                                        </li>
                                    }

                                }
                                <li class="page-item @(Model.Page == Model.TotalCount ? "disabled" : "")">
                                    <a class="page-link" asp-route-s="@Model.SearchKeyword" asp-route-p="@(Model.Page + 1)">
                                        Next
                                    </a>
                                </li>
                            </ul>
                        </nav>





                    </div>


                </div>

            </div>

        </main>
        <partial name="_NewFooter" />
    </div>
</div>

